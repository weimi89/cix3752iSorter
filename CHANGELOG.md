# 變更紀錄

本檔記錄各版本的修改與調整內容，標題版本號對應 GitHub Release tag。
`release.yml` 會依 tag 自動抽取對應段落，寫進 Release 說明與 `latest.json`（程式內「發現新版本」對話框顯示的就是它）。

## v0.2.7

### 讀碼站讀到鄰件條碼的件不再分錯口

- **同一幀有兩個條碼時取離畫面中心近的那個**：相機視野含進料區，下一件的 QR 在畫面上緣就被讀到，以前照字母序挑、有時把鄰件的單號綁到這件（這件分到別人的口、鄰件之後又被分一次）。**要到系統設定 → 讀碼站 →「畫面寬／高」填相機座標範圍才生效**（桃園約 4600×3400），沒填就照舊。
- **同碼幾秒內再進線直接送異常口**：同一條碼在 6 秒內剛正常分過又進線，不再問仲介機，直接走異常口讓人核對面單（原因顯示「同碼短時間再進線」）。秒數在系統設定 → 一般 →「同碼再進線攔截」，0 = 關閉。
- **讀碼失敗的件也會有照片**：上一件的面單還在畫面邊緣、這一件自己的碼沒讀到時，以前那一幀被當成重複略過、件要等綁碼窗口過了才判成讀碼失敗、照片也對不上；現在直接當讀碼失敗綁上去，照片對得到、格口也決定得更快。

### 班次報表：看一眼就知道要改什麼

- **新頁「班次報表」**（左側選單第二個）：一天一頁，頂部是進線、正常完成率、三類異常的大字；中間「問題點」由系統依門檻自動挑出、用現場的話寫，紅黃灰分級，每條有數字、有「可以做的」、有連結（翻照片或看統計）。門檻：讀碼失敗 3%／5%、格口完成後又進線 2%／4%、卡件每千件 5／10 次且點名佔三成以上的模組、分揀機異常 1.5%／3%、讀到鄰件條碼、仲介機回傳 10 件以上、投料空檔佔跨度 15% 以上、異常件標記不到一半。
- 打開報表沒選日期就是最近有件的那天；統計綜合與異常存證預設改成**昨天**（主管是事後看整個班次，今天的數字還在變）。
- 看板「異常件」卡下方的小字拿掉（沒人看小字）。

### 異常分三類看

- **統計綜合、看板、異常口處理清單都分「分揀機異常／仲介機回傳／讀碼失敗」**：分揀機異常是件沒到該去的口（失去追蹤、堵塞、取消），仲介機回傳是門市關轉、查無訂單、逾時這類仲介機說不能分的，讀碼失敗是沒讀到碼。三類互斥，趨勢圖四色堆疊。
- **完成率改成正常完成 ÷ 件數**：落到異常口的件不再算完成，數字會比以前低（9/21 從 97.9% 變 94.4%），這才是真的分到正常格口的比例。
- **「異常口處理」改成「異常存證」**：現場是多人快速作業，沒有人會停下來按「已重投／已下架」，那套流程拿掉。新頁面給主管事後翻：選日期，某一天所有異常件（讀碼失敗、仲介機回傳、分揀機異常）的存證照一格一格排開，點照片放大、點條碼看詳情；頁首有各類件數。
- **讀碼失敗的原因由系統判定**（照片看不出來的不做人工標記）：讀碼器沒讀到碼、讀到鄰件的碼、讀到的不是單號、讀碼器沒回應——從讀碼站回應判，存證頁可依原因篩、統計綜合與班次報表都有分布。
- 統計「依格口」每格多顯示「又進線」：在這格完成後同條碼十分鐘內再進線的件數，件其實沒落進去；集中在某幾格就是那幾格推包時機或落袋口有問題。

### 其他

- **讀碼站照片改成存原圖**：縮圖看不清楚，證據圖長邊預設改 0（存原圖）。原圖一張約 620 KB、一天約 5.7 GB，**升版後請到系統設定把照片保留天數調到 30 天以內**（既有設定檔的長邊值不會自動改，要一併填 0）。
- **照片放大檢視換成可縮放的檢視器**（與狸樂聚後台同款）：滾輪／手勢縮放、拖曳、1:1、旋轉、翻轉；「證據圖／原圖」切換拿掉，一律看最清楚的那張。
- 同一次卡件不再記兩筆「停線」（「堵塞到分揀機頭部」改記為「停線中」）。

## v0.2.6

### 照片與詳情完全分開、包裹詳情改版

- **照片按鈕直接放大**：包裹查詢的照片按鈕、異常口處理的縮圖，點了就是放大檢視（標題帶條碼、格口、時間，讀碼失敗件可切證據圖／原圖），不再先進詳情。
- **包裹詳情只看資料**：拿掉照片區。第一列條碼＋格口／來源／狀態，下面一列開始時間、耗時、小車、皮帶槽、回報 ID。
- **新增「流程」列**：上線 → 讀到條碼 → 取得格口 → 交接分揀機 → 上車 → 落格完成，每一步顯示時間與距上一步幾毫秒；堵塞／遺失的件最後一格標紅，一眼看出停在哪。
- **訊號時間軸改白話**：來源與訊號改成「皮帶／上線觸發」「分揀機／包裹上車」這類說法，原始內容退到最右欄小字。

## v0.2.5

### 讀碼站照片（修正與補強）

- **桌面視窗看不到照片**（只有破圖）：桌面版對後台是跨來源，圖片子資源會被跨站防護擋下；改成先取回再顯示，桌面與瀏覽器都正常。
- **照片可以跟資料庫分開放**：系統設定 → 讀碼站照片 → 「照片存放目錄」填絕對路徑（例如另一顆硬碟），空白維持在資料目錄下。換目錄不搬舊圖。
- **包裹查詢多了「操作」欄**：每列有「包裹詳情」與照片按鈕（沒照片的變灰），條碼旁不再放圖示。
- 放大檢視的讀碼失敗件可在「證據圖／原圖」之間切換。

## v0.2.4

### 讀碼站照片（新功能）：每件包裹留一張「我們有收到」的證據

- 讀碼器每件拍的照片用 FTP 上傳到分揀程式（程式內建 FTP 收圖，不用另裝軟體），收到後縮成證據圖存檔，原圖不留；讀碼失敗的件另外保留原圖，方便看清楚為什麼讀不到。
- **包裹詳情**多了「讀碼站照片」區：縮圖點開放大，讀碼失敗件可看原圖。**異常口處理**每列直接顯示縮圖；**包裹查詢**有照片的件在條碼旁標相機圖示。
- 照片照時間與檔名裡的條碼對回包裹；對不到的照片仍存檔並記事件。
- **系統設定 → 讀碼站照片**：開關、FTP 埠與帳密、被動模式埠範圍、證據圖大小與品質、對包裹時間窗口、照片保留天數（預設 90 天，與包裹資料分開清）。
- 升級後要在讀碼器（IDMVS）把 FTP 指到工控機並把存圖條件改成全部，並在工控機防火牆放行 FTP 埠；步驟見交接文件。

## v0.2.3

### 拿掉格口滿袋／換袋管理

- 現場格口下是多台大籠車，沒有「換袋」這回事，v0.2.2 的格口袋況卡、換袋按鈕、格口「每袋上限」與相關提示一律移除。
- 升級後資料庫自動收回相關表與欄位（`chute_bags`、`chutes.bag_limit`），不影響包裹、統計與其他資料。

## v0.2.2

### 格口滿袋／換袋管理（新功能）

- **格口對照**每個格口可設「每袋上限」（0 = 不限）。
- **看板新增「格口袋況」**：每格一塊，顯示本袋已落件數／上限、第幾袋與進度條；到 80% 轉黃、到上限整塊標紅，並記事件、看板提示、請中介機出聲「請換袋」（到上限那一件提醒，之後每多 5 件再提醒）。
- **換袋**：換好袋子後按該格口的「換袋」（會先確認），本袋件數從 0 重算、上一袋件數定版存檔；重啟程式後本袋件數照樣接著算。
- 手機版也能操作。

### 異常口處理清單（新功能）

- 側欄「現場 → 異常口處理」：今天落到異常口的包裹一張清單，顯示落格時間、條碼、原因（讀碼失敗／門市關轉／查無訂單…）、已等多久；待處理超過 10 分鐘整列標紅。
- 每件按「已重投」或「已下架」結案（依原因把建議的那顆按鈕放亮：讀碼失敗、逾時建議重投，關轉、查無建議下架），按錯可復原。
- **自動結案**：同一條碼再進線且中介機正常給格口，先前走異常口的那幾件自動記為「已重投（自動）」。
- 看板「異常件」卡下方顯示「異常口待處理 N 件 · 最久 X」，點卡片直接進清單。

### 現場提示（依 9/18 當天資料分析新增）

- **同一件反覆走異常口**：同條碼今天又因同一原因（門市關轉、查無訂單…）走異常口時，看板跳提示、事件記錄留一筆、並請中介機出聲「請下架不要再投」。9/18 有一件關轉包裹被投了 4 趟。
- **已完成卻又進線**：同條碼今天已分到正常格口完成，現在又進線 → 看板提示「請檢查該格口是否已滿或包裹被退回」（9/18 有 85 件），走異常口的重投不算。
- **同模組反覆卡件**：同一模組一小時內第 3 次卡件就提示「請檢查該段分流機構」，之後半小時內不重複（9/18 模組 3 一天卡 11 次）。
- **入口遺失**：包裹剛上車就在位置 1 遺失，事件記錄單獨標示（9/18 遺失 29 件有 9 件在入口）。

### 讀碼失敗

- **看板**：讀碼失敗卡改顯示近 1 小時失敗率，達 3% 以上整張標紅（9/18 一晚從 1.9% 爬到 3.6% 沒人發現）。
- **統計頁**：新增「讀碼失敗依包裹長度」圖：短件（長度 <15）失敗率是其他件的數倍，調整讀碼站後可對照。

### 連線

- **皮帶線閒置不再誤判斷線**：皮帶沒件時整段不說話又沒有查詢指令可當心跳，之前 300 秒沒資料就重連、重連時的「先停止再重置」還會把皮帶停下來（9/18 19:50 就是這樣停線）。改為只靠 TCP keepalive 判斷。
- **中介機查詢逾時保底放寬到 2.5 秒**：真正的截止點是包裹到交接點，之前 1.2 秒就砍掉還來得及的回覆。

### 事件記錄

- 類別「畫面」之外新增動作：同件重複異常、已完成又進線、卡件熱點、入口遺失。

## v0.2.1

### 即時看板

- **在途包裹表**：「開始時間」欄加寬到放得下 `19:38:50.319` 這種完整時間，不再被切掉；「已經過」欄也加寬，秒數破百不會少一位。
- **「已經過」不再出現負數**：從別台電腦開網頁版時，兩邊電腦時鐘常差一兩秒，之前直接拿瀏覽器的時間去減包裹進線時間就會算成 -787 ms。現在狀態快照帶著分揀機的時鐘，畫面照它算。

### 統計綜合

- **自動更新**：區間包含今天時每 30 秒在背景更新一次（不閃整頁的載入狀態），畫面標示「最後更新 時:分:秒 · 每 30 秒自動更新」；分頁切到背景就暫停、回到前景超過一輪立刻補一次；與後端斷線又接上也補一次。過往區間不會變動，不自動更新並標示說明。

### 網頁版

- 瀏覽器分頁標題改為「智配通 - 分揀控制」，與桌面視窗標題一致。

### 現場除錯能力

- **畫面錯誤回報**：桌面視窗或網頁版的 JavaScript 錯誤（元件出錯、未捕捉的例外、未處理的 Promise）自動送回後端，記進事件記錄「畫面」類別，附錯誤訊息、堆疊、出事頁面與環境。正式版桌面沒有開發者工具，元件默默不顯示時才有線索可查。同一則 30 秒內只記一次、每分鐘上限 30 則。
- **桌面版開啟網頁檢查器**：正式版也能在視窗內按右鍵「檢查元素」看 DOM 與主控台（只在本機視窗內可用，不對外開任何埠）。

## v0.2.0

### 新增「統計綜合」頁

- 側欄「現場 → 統計綜合」：選日期區間（今日／近 7 天／近 30 天或自訂）一次看齊件數、完成率、每日或每小時趨勢、依格口、格口來源、結束狀態、回報，以及近 7 天／近 30 天與前期的比較和星期 × 小時熱力圖。
- **異常原因**：走異常口或帶錯誤提示面單的原因（讀碼失敗、中介機逾時、回覆太晚、門市關轉、查無訂單、格口停用…）；原因代碼與中介機同一組，兩邊對得起來。升級前的舊資料只分得出讀碼失敗／逾時／其他。
- **卡件**：每次堵塞開始記一次，依分揀機模組（M1～M8）與時段看次數、每千件卡件率；升級時會把既有包裹的堵塞紀錄回填。
- **各段耗時**：相機綁碼、中介機回覆、分揀機受理、頭部交接、上線到落格口，各自的 p50／p90／p99／最長與超過預算的件數；落格口另依格口列出。
- **裝置穩定性**：皮帶、分揀機、讀碼站的斷線次數、最長一次、累計、最近一次與依時段分布。
- **重複進線**：同一條碼上線 2 次以上的條碼數、多跑次數與次數最多的清單（含每次分到的格口）。
- **讀碼失敗率趨勢**、**小車異常率**（明顯高於平均的小車標紅）。
- 這台沒印過面單就不顯示列印統計。
- 包裹明細只保留設定的天數，區間更早的日期只有每日件數，頁面會標示。

### 分揀機停線時不再每 5 分鐘誤判斷線

- 分揀機停線時不會主動說話，原本 5 分鐘沒收到資料就被當成斷線重連，整晚重連幾百次、每次還重置分揀機與亮一次告警燈。現在閒置 60 秒就送一句光電狀態查詢當心跳，它有回就不算斷線；真的斷電、拔線仍會在 5 分鐘內抓到。
- 斷線判定改成只認「收到資料」，自己送指令不再讓計時歸零。

### 其他調整

- 即時看板：在途包裹表固定欄寬、開始時間只顯示時分秒；「目前處理中」只顯示條碼。
- 包裹查詢、列印任務、回報佇列、事件記錄：日期預設不篩（原本預設今天）。
- 回報佇列與列印任務的操作欄加上「操作」表頭。
- 包裹明細 API 多回 `chute_reason`（原因代碼）。

### v0.1.1 之後已合併、尚未發版的

- 網頁後台可對外開放：內網免登入、外網要共用密碼（系統設定 → 網頁存取）。
- RWD 斷點統一為 640／768／1024／1280／1536。
- `sorter-switch`：修正 `to-new` 三個卡點與 `to-old` 刪不掉自動啟動檔的問題；`status` 改用 `sudo ss` 才看得到 root 跑的舊程式。
- 套件升級：ulid 3、vuetify 4.2.1、@vueuse/core 15、vue 3.5.43 等。

## v0.1.1

### 桌面版登入後自動啟動

- 工控機重開機、使用者登入後，「智配通 分揀控制」會自己開起來（舊系統靠 supervisor 做這件事，桌面版改由程式自己登記）。

### 只有桌面版

- 現場要在視窗上啟停皮帶，沒畫面的服務版（supervisor／systemd）用不到，整個拿掉：安裝只有 `sudo bash install.sh` 一種，Release 只剩安裝包、.deb、`latest.json` 三個檔。

### 一鍵切換工具 `sorter-switch`

- 安裝後多一支 `sorter-switch`：`to-new` 停舊程式、確認相機與分揀機都放掉、舊的改成不自啟、開新程式、等裝置連上；`to-old` 反向兩分鐘回退；`status` 看現在誰在跑。每一步有檢查，卡住會停下來印原因。

## v0.1.0

### 首次發版：Rust 重寫版取代廠商的 main_proj

一支程式接皮帶線、分揀機、讀碼站，向中介機取格口與面單、下分揀指令、列印面單、追蹤每一件包裹，並提供網頁後台與手機遙控頁。Ubuntu 20.04 工控機用，桌面視窗與無畫面服務二選一。

### 與舊系統不同、現場會感覺到的

- **斷線不再當機、重連不再重複下指令**：皮帶或分揀機拔線再插回，程式自己重連並先停止皮帶、重置分揀機。
- **重啟不丟件**：包裹一上線就落資料庫，程式重啟後在途件會標為「失去追蹤」而不是消失。
- **取消件、異常件查得到**：舊系統啟動時會把這些件的詳情誤刪，新版依上線時間保留 15 天。
- **面單先抓到才分到正常格口**：中介機有給格口但面單抓不到的件，改走異常口且不回報，不會出現包裹到了卻沒面單可貼。
- **看板即時**：裝置燈、皮帶啟停、今日件數、目前處理中、在途包裹、近 12 小時件數、中介機查詢延遲（p50／p90／p99）、系統訊息，0.5 秒更新。
- **包裹查詢**：每件的訊號時間軸，超時區段（下指令後遲遲沒受理、上車後遲遲沒離開頭部、格口回覆太晚）標紅並附說明；可匯出 Excel。
- **列印任務**：待印／失敗的面單可先預覽再補印；印表機掉線自動重試並通知中介機。
- **光電檢查**：每台分揀機一格看光電狀態，點開看每顆光電，壞掉的可先屏蔽整台讓線能跑。
- **手機遙控**：導覽列掃 QR，同一個 Wi-Fi 的手機就能在線邊看狀態、啟停皮帶。
- **日誌留檔**：程式日誌與裝置原始訊號逐日一檔，網頁「事件記錄」可直接下載，不用進工控機撈。
- **自動更新**：程式內「發現新版本」一鍵更新。

### 設定

- 首次啟動自動產生 `config.toml`，預設值就是現場的裝置位址、指令、燈號、列印紙張與格口對照，不用另外轉換。
- 網頁後台預設埠 `18090`（避開舊系統的 8080）；不設操作密碼。
